# Cinematic Hero

Scaffolded Vite + React + TypeScript app with Tailwind CSS.

Install and run:

```bash
npm install
npm run dev
```

The hero uses a looping video background with custom fade-in/fade-out logic implemented in `src/components/Hero.tsx`.
