import { FormEvent, useState } from 'react';
import { ArrowLeft, KeyRound } from 'lucide-react';
import supabase from '../lib/supabase';
export default function ResetPassword(){const[p,setP]=useState(''),[confirm,setConfirm]=useState(''),[message,setMessage]=useState(''),[error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();setError('');if(p.length<6||p!==confirm){setError('Passwords must match and contain at least 6 characters.');return}const{error}=await supabase.auth.updateUser({password:p});if(error){setError(error.message);return}setMessage('Password updated. You can now sign in.')} ;return <main className="auth-page"><a href="/sign-in" className="auth-back"><ArrowLeft size={16}/> Sign in</a><section className="reset-card"><div className="auth-icon"><KeyRound size={21}/></div><span className="eyebrow">SECURE PASSWORD RESET</span><h1>Choose a new password.</h1><p>Use a secure password you have not used elsewhere.</p><form onSubmit={submit}><label>New password<input type="password" value={p} onChange={e=>setP(e.target.value)}/></label><label>Confirm new password<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></label>{error&&<div className="auth-error">{error}</div>}{message&&<div className="auth-success">{message}</div>}<button className="auth-submit">Update password</button></form></section></main>}
