import { FormEvent, useEffect, useState } from 'react';
import { ArrowDownToLine, Bot, Check, LoaderCircle, Sparkles, Wand2 } from 'lucide-react';
import { useAuth } from '../contexts/AuthContext';
import supabase from '../lib/supabase';
type Cv={id:number;title:string;content:{name:string;headline:string;location:string;email:string;about:string;skills:string[];highlights:string[];experience?:string;education?:string}};type Details={skills:string;experience:string;education:string;about:string};
const getHeaders=async()=>{const {data:{session}}=await supabase.auth.getSession();return {'Content-Type':'application/json',Authorization:`Bearer ${session?.access_token||''}`}};
function printCv(cv:Cv){const c=cv.content;const w=window.open('','_blank','width=900,height=1000');if(!w)return;w.document.write(`<!doctype html><html><head><title>${c.name} CV</title><style>@page{size:A4;margin:0}*{box-sizing:border-box}body{margin:0;font-family:Arial,sans-serif;color:#15392f}.page{min-height:1123px;display:grid;grid-template-columns:31% 69%}.side{background:#183c35;color:#eff5e8;padding:52px 30px}.main{padding:55px 48px}.badge{font-size:10px;letter-spacing:2px;color:#d9e86d}.name{font:700 35px Georgia,serif;line-height:1.05;margin:14px 0 8px}.role{font-size:14px;color:#d9e86d;line-height:1.5}.contact{border-top:1px solid #ffffff33;margin-top:32px;padding-top:20px;font-size:12px;line-height:1.8;color:#d3e0d7}.section{margin-bottom:27px}.side h3,.main h3{font-size:10px;letter-spacing:1.7px;margin:0 0 11px}.side h3{color:#d9e86d}.main h3{color:#6f8428}.skills span{display:block;font-size:12px;padding:7px 0;border-bottom:1px solid #ffffff20}.intro{font:19px/1.55 Georgia,serif;margin:0 0 34px}.main p,.main li{font-size:13px;line-height:1.65;color:#49665c}.main ul{padding-left:19px;margin:0}.entry{border-left:2px solid #d9e86d;padding-left:14px;margin:0 0 17px;white-space:pre-wrap}.entry strong{font-size:13px}@media print{body{print-color-adjust:exact;-webkit-print-color-adjust:exact}}</style></head><body><div class="page"><aside class="side"><div class="badge">ORBIT CAREERS</div><div class="name">${c.name}</div><div class="role">${c.headline}</div><div class="contact">${c.location}<br>${c.email}</div><div class="section" style="margin-top:42px"><h3>CORE SKILLS</h3><div class="skills">${c.skills.map(s=>`<span>${s}</span>`).join('')}</div></div></aside><main class="main"><div class="section"><h3>PROFESSIONAL PROFILE</h3><p class="intro">${c.about}</p></div><div class="section"><h3>EXPERIENCE</h3><div class="entry">${c.experience||'Add your experience in the Orbit AI Studio to complete this section.'}</div></div><div class="section"><h3>EDUCATION</h3><div class="entry">${c.education||'Add your education in the Orbit AI Studio to complete this section.'}</div></div><div class="section"><h3>CAREER STRENGTHS</h3><ul>${c.highlights.map(h=>`<li>${h}</li>`).join('')}</ul></div></main></div><script>window.onload=()=>window.print()<\/script></body></html>`);w.document.close()}
export default function AiCvStudio(){const{user}=useAuth();const[items,setItems]=useState<Cv[]>([]),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[details,setDetails]=useState<Details>({skills:'',experience:'',education:'',about:''});const load=async()=>{if(!user){setItems([]);return}const r=await fetch('/api/cv-generator',{headers:await getHeaders()});if(r.ok)setItems(await r.json())};useEffect(()=>{load()},[user]);const create=async(e:FormEvent)=>{e.preventDefault();if(!user){setMessage('Sign in, then complete your profile so Orbit AI can create your CV.');return}setBusy(true);setMessage('Orbit AI is writing and designing your CV…');try{const r=await fetch('/api/cv-generator',{method:'POST',headers:await getHeaders(),body:JSON.stringify(details)});const d=await r.json();if(!r.ok)throw new Error(d.error);setItems(x=>[d,...x]);setMessage('Your premium CV is ready. Select download to save it as a PDF.')}catch(e){setMessage(e instanceof Error?e.message:'We could not create your CV.')}finally{setBusy(false)}};const cv=items[0];return <section className="ai-studio" id="ai-cv"><div className="studio-copy"><div className="eyebrow"><Bot size={15}/>ORBIT AI CV STUDIO</div><h2>A CV designed to<br/><em>open doors.</em></h2><p>Give Orbit the important details. It creates a polished, print-ready CV that reflects your experience — not a generic template.</p><form className="cv-details" onSubmit={create}><label>Skills <input value={details.skills} onChange={e=>setDetails({...details,skills:e.target.value})} placeholder="React, Figma, SQL, communication"/></label><label>Experience <textarea value={details.experience} onChange={e=>setDetails({...details,experience:e.target.value})} placeholder="Product Designer — Studio Name | 2022–Present&#10;Describe your impact, projects, and outcomes."/></label><label>Education <input value={details.education} onChange={e=>setDetails({...details,education:e.target.value})} placeholder="B.Des, University Name | 2022"/></label><button className="ai-button" disabled={busy}>{busy?<LoaderCircle className="spin" size={17}/>:<Wand2 size={17}/>} {busy?'Crafting your CV…':'Create premium CV'}</button></form>{message&&<div className="ai-message">{message}</div>}<div className="studio-points"><span><Check size={15}/>Uses your profile</span><span><Check size={15}/>Print-ready PDF layout</span></div></div><div className="cv-preview">{cv?<><div className="preview-accent"/><span className="preview-label">PREMIUM CV</span><h3>{cv.content.name}</h3><p className="preview-headline">{cv.content.headline}</p><div className="preview-line"/><strong>PROFILE</strong><p>{cv.content.about}</p><strong>CORE SKILLS</strong><div className="preview-skills">{cv.content.skills.slice(0,5).map(s=><span key={s}>{s}</span>)}</div><button onClick={()=>printCv(cv)} className="download-cv"><ArrowDownToLine size={17}/>Download as PDF</button></>:<div className="preview-empty"><Sparkles size={23}/><strong>Your premium CV</strong><p>Complete your details, then let Orbit turn them into a polished document.</p></div>}</div></section>}
